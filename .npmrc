# Publish scoped packages publicly by default (no per-call --access needed).
access=public

# Fail if a required field is missing instead of silently publishing.
# (Set the auth token via `npm login` or CI secrets before publishing.)
